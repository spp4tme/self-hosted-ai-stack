FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    mem0ai crewai crewai-tools langchain-openai \
    fastapi uvicorn[standard] httpx aiohttp \
    qdrant-client openai python-dotenv \
    structlog PyGithub

COPY . .

CMD ["python", "-m", "agents.crew"]
